Put your song here as: song.mp3

That's it — src/data/content.ts already points to "/music/song.mp3".
If you want a different filename, update `music.src` in that file to match.

No file yet? The music toggle will just silently do nothing until you add one.
